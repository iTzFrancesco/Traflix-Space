# Optional local development configuration for Jarvis voice transcription.
# The desktop application can also manage this value through its settings.
# Never put a real credential in this file or commit a populated .env file.
# Groq Speech-to-Text keys start with gsk_; xAI/Grok keys are not compatible.
GROQ_API_KEY=
